import { IUser } from './IUser';

export interface IWord {
    ua: string;
    en: string;
    _id: string;
}

export type Word = {
    ua: string;
    en: string;
}

export type IPost = {
    ua: string;
    en: string;
    _id: string;
}

